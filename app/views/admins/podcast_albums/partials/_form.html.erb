<%= form_with(model: podcast_album) do |form| %>
  <% if podcast_album.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(podcast_album.errors.count, "error") %> prohibited this podcast_album from being saved:</h2>

      <ul>
        <% podcast_album.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
