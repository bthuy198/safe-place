<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Safe Place</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "users/style", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <% if user_signed_in? %>
      <style>
        <%= ".msg_#{current_user.id}"%> {
          display: flex;
          flex-direction: row-reverse
        }
        <%= ".bg_#{current_user.id}"%> {
          background-color: rgba(57, 192, 237,.2);
          padding: 5px 15px;
          border-radius: 15px
        }
      </style>
    <% end %>
    
    
  </head>

  <body id="body" class="d-flex flex-column min-vh-100" data-controller="frame-moving">
    <%= render "shared/flash"%>
    <%= yield %>
    <%= render "layouts/users_layout/partials/footer"%>
  </body>

</html>
