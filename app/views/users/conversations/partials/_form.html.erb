<%= form_with(model: [@room, Conversation.new], url: users_room_conversations_path(@room), id: "form_comment") do |form| %>
        <%= form.text_area :content, class: "comment_textfield"%>
        <%= form.submit "Send",class: "btn btn-primary comment_submit"%> 
<% end %>