<%= form_with(model: [@room, Conversation.new], url: users_room_conversations_path(@room)) do |form| %>
                            <%= form.rich_text_area :content %>
                            <%= form.submit%>
                    <% end %>