<%= form_with(model: confession, url: url) do |form| %>
  <% if confession.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(confession.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
        <% confession.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tag, style: "display: block" %>
    <%= form.text_field :tag %>
  </div>

    <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.rich_text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
