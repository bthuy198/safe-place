<div id="<%= dom_id podcast_album %>">
    <div class="col-3">
            <div class="card item">
                <div class="card-body d-flex flex-column justify-content-center">
                        <%= podcast.image? ? image_tag(podcast.image_url, class: "podcast_image") :  image_tag("podcast_default.jpg", class: "podcast_image") %>
                        <div class="podcast_name"><%=podcast.name%></div>
                        <div class="podcast_author"><%=podcast.author_name%></div>
                    <div class="album_name"><%=podcast.podcast_album.name%></div>
                </div>
            </div>
    </div>
</div>
